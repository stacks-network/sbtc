[workspace]
resolver = "2"
members = [
  "signer",
  "blocklist-client"
]

[profile.dev]
# Disabling debug info speeds up builds a bunch,
# and we don't rely on it for debugging that much.
debug = 0

[profile.release]
lto = "thin"
codegen-units = 16
