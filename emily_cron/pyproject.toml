[project]
name = "emily-cron"
version = "0.1.0"
description = "Cron job for updating deposit statuses in Emily"
requires-python = ">=3.13"
dependencies = [
    "bitcoinlib==0.7.2",
    "embit==0.8.0",
    "requests==2.33.1",
]
